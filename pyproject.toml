[project]
name = "semantic-search-platform"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "chromadb>=1.5.9",
    "datasets>=5.0.1",
    "pytrec-eval>=0.5",
    "sentence-transformers>=5.7.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
